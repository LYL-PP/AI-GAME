# M0 文案资产 · 交付报告

日期：2026-07-17 ｜ 阶段：M0 预制作 ｜ 状态：**完成**

## 1. 交付内容

### 1.1 剧情文案 JSON（data/，全部可被引擎读取）

| 文件 | 内容 | 规模 |
|---|---|---|
| `characters.json` | 11 角色档案（10 NPC + 玩家）：外貌/性格/声线/指控/真相/死亡/伏笔/模型与立绘指引 | 11 人 |
| `rhyme.json` | 童谣十句（大纲 4.4 原创译文）+ 死者/章节/手法映射 | 10 句 |
| `accusation.json` | 留声机指控完整播放稿：开场白 + 11 条指控（含原创第 11 条·玩家）+ 结束语 | 约 700 字 |
| `chapters.json` | 12 章节卡：标题/副标题/童谣句/存活数/天气预设/氛围/日期 | 12 章 |
| `deaths.json` | 10 死亡事件：手法/现场/发现方式/线索关联/声效引导/呈现方式 | 10 起 |
| `clues.json` | 13 条线索（速记体，只陈述事实）+ 推理三链（A 假死 / B 动机 / C 排除） | 13 条 |
| `confession.json` | 沃格雷夫自白信五段（动机→选人→行刑→假死→落幕），每段挂闪回 echoId | 约 1100 字 |
| `endings.json` | 4 结局：触发条件 + 字幕文案 | 4 个 |
| `ui.json` | 系统 UI 文案：操作引导/笔记本/章节卡/死亡横幅/指认/统计/系统 | 6 组 |
| `dialogues/*.json` | 对话树：11 文件（10 NPC + echoes），按章节组织 | **261 节点** |

### 1.2 对话树覆盖（与死亡表严格对齐，校验器自动核对）

| NPC | 章节 | 节点数 |
|---|---|---|
| wargrave | 0–5（第 6 章假死） | 26 |
| vera | 0–10（含第 9/10 章独白） | 52 |
| lombard | 0–8 | 40 |
| armstrong | 0–6 | 33 |
| blore | 0–7 | 37 |
| macarthur | 0–2 | 12 |
| brent | 0–4 | 21 |
| rogers | 0–3 | 17 |
| mrs_rogers | 0–1 | 8 |
| marston | 0 | 5 |
| echoes（结局闪回） | 11 | 10 |
| **合计** | | **261**（目标 250–350 ✔） |

每章每人 3–5 主流程节点 + 线索追问节点（requireClue 门控）+ 第 3 章起 suspect_reaction 节点。
关键 flag 已埋：`un_owen_revealed`、`lombard_confessed_africa`、`blore_confessed_landor`、`armstrong_certified_death`（链 A 验尸）、`blore_doubted_autopsy`、`blore_heard_steps` 等。

### 1.3 美术资产

- `assets/portraits/*.svg` — 11 张占位立绘（Art Deco 边框 + 剪影 + 名牌，画风统一）
- `docs/立绘与参考图提示词.md` — 正式立绘统一提示词模板 + 11 角色提示词 + 瓷人/别墅参考图提示词

### 1.4 工具（tools/）

- `validate_data.py` — 数据校验（引用完整性、死亡顺序=童谣顺序、对话指针闭合、章节不越死亡章）
- `merge_dialogues.py` — 对话分片合并（已完成使命，保留备用）
- `make_portraits.py` — 占位立绘生成器
- `preview.html` — 全部文案的可视化审阅页（章节/童谣/指控/人物/死亡/线索/对话树/自白信/结局/UI）

## 2. 运行方式

```bash
cd game-正式版
python -m http.server 8000
# 浏览器打开 http://localhost:8000/tools/preview.html 审阅全部文案
python tools/validate_data.py   # 数据校验
```

## 3. 验收标准逐项自检（大纲 §8 M0）

| 标准 | 结果 |
|---|---|
| 产出大纲 §5 全部文案资产 | ✔ 童谣 / 指控全文 / 对话树 261 段 / 13 线索描述 / 自白信五段 / 章节卡 / UI 文案 |
| 所有 JSON 可被引擎读取 | ✔ `validate_data.py` 全绿（0 错误 0 警告）：引用、顺序、指针、章节边界全部核对 |
| 立绘画风统一 | ✔ 占位立绘统一模板生成（SVG）；正式立绘提示词模板已备，待用户用绘图工具生成替换 |

## 4. 忠于原著的核对记录

- 死亡顺序/手法/童谣映射：与原著及大纲 2.3/4.5 逐项一致（校验器强制 `deaths 顺序 = rhyme 顺序`）
- 指控事实（人名/日期/受害者）：逐条取自原著第三章，措辞原创（大纲 §5 要求不引用现有中译本）
- 自白信事实线（莫里斯灭口、按罪轻重排序、氰化钾/曲砜那、假死合谋、悬崖、熊钟、橡皮筋机关、该隐记号）：与原著尾声一致，行文原创
- 链 A 关键对话已落实：阿姆斯特朗第 6 章"独自验看、一枪毙命"、布洛尔第 7 章 clue_08 追问
- 人名统一以本项目数据为准（沃格雷夫/马尔斯顿），与各中译本差异属有意为之
- 依据材料：`研读笔记/第1–4部分`（全书四部分精读，含关键原文逐字抄录）

## 5. 遗留事项（不阻塞 M1）

1. **正式立绘**：占位 SVG 可支撑开发；正式版需用户用提示词文档自行生成（我无 AI 绘图能力）
2. **瓷人/留声机/家具 3D 模型**：素材缺口已确认（现有 Kenney 包不含），M1 先用程序化几何体占位，或用户补充素材
3. **音频**：M6 阶段处理
4. 大纲建议 M1→M3 先做"能玩到底"的骨架，推理深度在 M4/M5 回填——M0 数据已按最终形态备齐，M4/M5 无需返工文案
